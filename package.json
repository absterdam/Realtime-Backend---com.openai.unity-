{
  "name": "openai-realtime-unity-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Backend for the OpenAI Unity Realtime sample to mint client secrets and proxy image generation.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}